# dataset-helpers

## Font Dataset
Generate a dataset to train a LoRA for fonts. Get the .ttf file and run the script.

```
python3 font_dataset.py Roboto.ttf
```

eg.
![example dataset image](img/Roboto-Bold.png)